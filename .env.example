# Local MySQL (XAMPP)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=vaiexpress
DB_PORT=3306

# Or use a single connection string (recommended for cloud DB)
# Example: mysql://user:pass@host:3306/vaiexpress
DB_URL=

# AWS Secrets Manager (optional)
# SECRETS_MANAGER_SECRET_ID=your-secret-id-or-arn
# AWS_REGION=ap-southeast-1

# AWS SSM Parameters (optional, comma separated names; last path segment becomes env key)
# SSM_PARAMS=/prod/DB_URL,/prod/DB_HOST,/prod/DB_USER,/prod/DB_PASS,/prod/DB_NAME
